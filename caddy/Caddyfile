https://{$FQDN}
tls {$EMAIL}

gzip
log stdout
errors stdout

git {
  repo {$REPO}
  branch {$BRANCH}
  hook /caddy-git-deploy-hook {$WEBHOOK_SECRET}
}

proxy /curriculum-vitae https://evansosenko.github.io
